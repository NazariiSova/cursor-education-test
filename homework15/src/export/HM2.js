function getNumder(){
    let N = Number(prompt("Користувачу, введіть ціле число(N) від якого ми будемо складати."));

while (N % 1 !== 0 || N == "" || N <= 0) {
    N = Number(prompt("Користувачу, це число нам не підходить! Введіть інше ціле число(N) від якого ми будемо складати."));
}
return;
};